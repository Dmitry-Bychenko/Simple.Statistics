using Microsoft.VisualStudio.TestTools.UnitTesting;
using Simple.Statistics.Distributions.Library;

namespace Simple.Statistics.Tests {

  /// <summary>
  /// 
  /// </summary>
  [TestClass]
  public class LinearRegressionTest {

    //[TestMethod]
    //public void Demo() {
    //  var dist = new StudentDistribution(7);

    //  var v = dist.Qdf(0.99);

    //  //var v = StudentDistribution.Qdf(0.99, 7);

    //  Assert.Fail($"{v}");
    //}

    //[TestMethod]
    //public void Parabolic() {
    //  (int x, int y)[] data = new (int x, int y)[] {
    //    (1, 1),
    //    (2, 4),
    //    (3, 9),
    //    (4, 16),
    //    (5, 25),
    //    (6, 36),
    //    (7, 49),
    //    (8, 64),
    //    (9, 81),
    //  };

    //  var result = data.ToLinearRegression(item => item.x, item => item.y);

    //  string message = $"{result.A} * x + {result.B} ({result.R * result.R}) : {result.ESS / result.TSS}";

    //  Assert.Fail(message);
    //}

  }

}
